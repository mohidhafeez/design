import 'package:flutter/material.dart';

class NaviationView extends StatelessWidget {
  const NaviationView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
